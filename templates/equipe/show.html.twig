{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe de {{ equipe.dresseur }}</h1>
    <div style="display: flex;
    flex-direction: row;
    justify-content: flex-start;
    align-items: baseline;">

        <td>Equipe n°{{ equipe.id }}</td>
        <td><img src="{{ equipe.pokemon_1.sprite }}" alt="{{ equipe.pokemon_1 }}"/>
            <h2>{{ equipe.pokemon_1.nom }}</h2></td>

        <td><img src="{{ equipe.pokemon_2.sprite }}" alt="{{ equipe.pokemon_2 }}"/>
            <h2>{{ equipe.pokemon_2.nom }}</h2></td>

        <td><img src="{{ equipe.pokemon_3.sprite }}" alt="{{ equipe.pokemon_3 }}"/>
            <h2>{{ equipe.pokemon_3.nom }}</h2></td>

        <td><img src="{{ equipe.pokemon_4.sprite }}" alt="{{ equipe.pokemon_4 }}"/>
            <h2>{{ equipe.pokemon_4.nom }}</h2></td>

        <td><img src="{{ equipe.pokemon_5.sprite }}" alt="{{ equipe.pokemon_5 }}"/>
            <h2>{{ equipe.pokemon_5.nom }}</h2></td>

        <td><img src="{{ equipe.pokemon_6.sprite }}" alt="{{ equipe.pokemon_6 }}"/>
            <h2>{{ equipe.pokemon_6.nom }}</h2></td>
    </div>


    <a href="{{ path('app_equipe_index') }}">Retour à la liste</a>

    <a href="{{ path('app_equipe_edit', {'id': equipe.id}) }}">Modifier</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
